# Scholar-Agent 新手全景导读（单文件百科）

> **一句话**：给一句话（"帮我复现 Attention 那篇论文"），系统识别意图 → 规划步骤 → **人工审批** → 在 **Docker 沙箱**里搜仓库、装依赖、跑代码 → 跑挂了让 LLM 修但**过 5 道闸验收** → 出一份复现报告。全程 SSE 直播。
>
> 本文把整个项目从头到尾捋一遍：每个模块有完整链路，关键代码原样贴出并逐段讲作用。读完这一份，等于把 README + 源码走读 + 面试速答全部过了一遍。

---

## 📖 这本书怎么读

| 你的目标 | 读法 |
|---|---|
| **面试速通**（30 分钟） | 第 1 章 → 第 6 章 → 第 9 章（面试速答） |
| **新手通读**（半天） | 从头按顺序读，第 5 章的八站旅程是主线 |
| **源码精读** | 读第 5 章时对照第 10 章的"阅读路线"打开真实文件 |

**章节地图**：

| 章 | 内容 | 重要度 |
|---|---|---|
| 1 | 一分钟看懂（心智模型 + 全流程图） | ⭐⭐⭐ |
| 2 | 技术栈与选型理由 | ⭐⭐ |
| 3 | 全景架构（主图 / 目录 / 数据血缘） | ⭐⭐⭐ |
| 4 | 数据模型四件套（含代码逐行） | ⭐⭐⭐ |
| 5 | 一次请求的完整旅程（八站，全链路源码） | ⭐⭐⭐ |
| 6 | 三大亮点深挖（约束 LLM / 沙箱 / Harness） | ⭐⭐⭐ |
| 7 | 测试体系（106 条怎么组织的） | ⭐⭐ |
| 8 | CI 与部署 | ⭐⭐ |
| 9 | 面试速答（10 问 + 追问表） | ⭐⭐⭐ |
| 10 | 命令速查 + 源码阅读路线 | ⭐ |

---

## 第 1 章 一分钟看懂

### 1.1 它解决什么问题

论文复现是一件很痛的事：读完论文 → 翻 GitHub 找实现 → clone → 配环境 → 跑 → 报错 → 人肉修 → 再跑……**Scholar-Agent 把这条流水线做成了自动化**，但它和"让 LLM 随便写代码"有本质区别：

```
LLM 只负责"提议"（建议什么步骤、建议怎么修代码）
代码负责"裁决"（派给谁、按什么顺序、算不算通过、跑没跑成）
人负责"批准"（执行前看一眼计划，approve / revise / abandon）
```

### 1.2 最小心智模型（先记住这四个角色）

| 角色 | 对应 | 一句话 |
|---|---|---|
| **包工头** | `planner`（LLM 提议 + 代码验收） | 把"复现论文"拆成若干**步骤**（工单） |
| **工单** | `Step` + `Plan` | 每步写清：做什么、派给谁、领什么料、交什么货 |
| **业主** | 人（`approval` 节点） | 开工前审计划：同意 / 打回重拟 / 放弃 |
| **牢房 + 监理** | `Docker 沙箱` + `Harness` | 不可信代码在牢房里跑；跑挂了自己修，但每次修复过 5 道闸 |

### 1.3 全流程图

```
用户一句话
   │
   ▼
POST /runs ──► [intent] 意图识别（规则快路径 + 三路并行 LLM）
   │                │ 意图不合格 → END（SSE 收到 plan_failed 收流）
   ▼
 [planner] 规划：LLM 出步骤 → 两道闸验收 → 不过就模板兜底
   │
   ▼
 [approval] ⭐ interrupt() 挂起整张图 —— 等人
   │            │ revise（带意见回炉）→ 回 planner
   │            │ abandon → report（终态 canceled）
   ▼ approve
 [execute_step] 自环：按下标取一步 → 派工给 worker → 成功游标+1 再来
   │                │ 失败 3 次 → fail-fast → report
   │                │ /cancel 标记 → 剩余步骤标记取消 → report
   │   ┌────────────────────────────────────────┐
   │   │ worker 内部（以 research_coding 为例）： │
   │   │  搜 GitHub repo → 沙箱装依赖(联网)      │
   │   │  → 切换断网容器 → 跑入口文件            │
   │   │  → 跑挂 → Harness 3 轮自愈（5 道闸）    │
   │   └────────────────────────────────────────┘
   ▼
 [report] 终态判定（取消 > 全成功 > 失败）+ 拼报告 → END
```

全程事件走 **SSE**（`/plans/{id}/stream`）实时推给前端；状态唯一权威是 **LangGraph checkpoint**（`GET /plans/{id}` 随时可查）。

---

## 第 2 章 技术栈

| 技术 | 版本 | 干什么 | 关键位置 |
|---|---|---|---|
| Python | ≥3.12 | 运行环境（用到了 `StrEnum` 时代的写法、`UTC` 常量等 3.11+ 特性） | `pyproject.toml` |
| **uv** | — | 包管理 + 锁文件（`uv.lock`），本地与 CI 同一份依赖树 | `uv.lock` |
| FastAPI + uvicorn | ≥0.115 | HTTP 层：5 个端点 + SSE | `scholar_agent/main.py` / `api/` |
| **LangGraph** | ≥0.2.50 | 编排核心：5 节点状态图 + `interrupt()` + checkpointer | `scholar_agent/graph.py` |
| langgraph-checkpoint-redis | ≥0.0.1 | 生产级持久化（跨重启、审批可恢复）；失败自动降级 InMemory | `main.py::_build_checkpointer` |
| langchain-openai | ≥0.2 | LLM 客户端（OpenAI 兼容协议，默认 DeepSeek） | `agent/llm_client.py` |
| **docker-py** | ≥7.1 | 沙箱：起容器、两阶段网络、断网执行 | `agent/sandbox/docker_executor.py` |
| Redis | ≥5.2 | 三件事：意图记忆 / 沙箱容器注册表 / checkpoint | `intent/memory.py`、`sandbox/registry.py` |
| pydantic + pydantic-settings | ≥2.10 | 数据模型 + 配置（含生产强校验） | `models/`、`core/config.py` |
| PyGithub | ≥2.5 | 搜 repo（star/活跃度质量过滤） | `agent/tools/github_search.py` |
| structlog | ≥24.4 | 结构化日志 | `core/logging.py` |
| pytest(+asyncio/cov) / fakeredis / ruff / mypy | dev | 测试与质量（106 条全离线） | `tests/`、`pyproject.toml` |

**选型总原则："能力跟模块走，决策跟图走"**——每个危险/不确定的外部边界都放在独立模块里（沙箱、Harness、依赖安装……），图节点只调用不搬运。

---

## 第 3 章 全景架构

### 3.1 主图：5 个节点 + 两种导航方式

```
intent ──Command──► planner ──(静态边)──► approval ──approve──► execute_step ──┐
  │ （plan 已存在：图级直连入口）              ▲（interrupt 挂起）  ▲            │（自环：游标+1）
  │  └──Command──► execute_step                │（revise：回炉      │            │
  └─（意图不合格）─Command──► END              │  重新规划）        │            │
                                              └─ abandon ─► report ◄────────────┘
                                                             ▲（走完/失败/取消）
                                                         report ──► END
```

| 节点 | 职责 | LLM 参与度 |
|---|---|---|
| `intent` | 意图识别（规则快路径 + 三路并行 LLM） | 高（三路调用） |
| `planner` | 规划（LLM 优先 → 两道闸 → 模板兜底） | 中（提议在 LLM，验收在代码） |
| `approval` | 人工审批（`interrupt()` 挂起整图） | 零（等人） |
| `execute_step` | 执行一步（自环推进游标） | 零（worker 墙内才可能有 LLM） |
| `report` | 收尾（定终态 + 拼报告） | 零 |

**导航分工**（这是 LangGraph 的关键手法）：
- **动态分流节点**（`intent` / `approval` / `execute_step`）返回 `Command(goto=..., update=...)`——去向和 state 更新在**同一个 return 里原子表达**，不挂静态出边；
- **固定路径**（`planner→approval`、`report→END`）用普通静态边。

### 3.2 目录结构（每个文件一句话）

```
scholar_agent/
├── graph.py                  ⭐ 主图：5 节点 + execute_step 自环 + 事件发射
├── state.py                  AgentState（游标 current_step + revision_count reducer）
├── main.py                   FastAPI 装配（lifespan 组装依赖 + 惰性连接 + 降级）
├── models/
│   ├── step.py               ⭐ Step = 一张工单（设计字段 vs 运行时字段分界）
│   ├── plan.py               Plan = 工单本（steps）+ 仓库（artifacts）
│   ├── artifact.py           Artifact = Agent 之间的交接棒
│   ├── intent.py             IntentContext / IntentType（含 PaperSearchFields）
│   ├── event.py              PlanEvent（SSE 事件模型）
│   └── harness.py            HarnessAttempt / HarnessReport
├── agent/
│   ├── llm_client.py         LLM 客户端封装（ainvoke + with_structured_output）
│   ├── intent/
│   │   ├── classifier.py     ⭐ 三路并行意图识别 + 独立降级
│   │   ├── rule_router.py    规则快速路径（关键词命中 0 次 LLM）
│   │   └── memory.py         Redis 意图记忆（不可用降级 Noop）
│   ├── planner/
│   │   ├── planner.py        规划主入口（LLM 优先 + 模板兜底）
│   │   ├── agent_planner.py  LLM 规划（Structured Outputs 出 PlanBlueprint）
│   │   ├── validator.py      ⭐ 两道闸：产物契约闭合 + 高契约派工
│   │   └── templates.py      确定性模板（永远可用的兜底）
│   ├── workers/              4 个异构 Agent（见 6.2）
│   ├── sandbox/
│   │   ├── docker_executor.py ⭐ 四层隔离 + 两阶段网络 + 持久化容器
│   │   ├── registry.py       容器索引（Redis，崩溃恢复用）
│   │   ├── security.py       路径逃逸校验 / 镜像白名单 / 安全参数三件套
│   │   └── result.py         SandboxResult
│   ├── harness/
│   │   ├── runner.py         ⭐ 自愈主循环（3 轮 + 5 道闸）
│   │   ├── patch_policy.py   闸1：补丁静态校验（防越权）
│   │   ├── fingerprint.py    闸2/3：SHA256 假修复检测 + 仓库指纹
│   │   ├── metrics_recompute.py 闸4：指标重算防伪
│   │   ├── circuit_breaker.py   闸5：熔断器（防死循环烧 token）
│   │   └── feedback_injector.py 负反馈注入（修复 prompt 带上历史失败）
│   ├── tools/
│   │   ├── github_search.py      repo 搜索 + star/活跃度过滤
│   │   └── dependency_installer.py 依赖安装 + 失败降级
│   └── prompts/              各处 system/user prompt
├── api/
│   ├── routes.py             ⭐ 5 端点 + 双击守卫 + 三态取消
│   ├── sse.py                EventBus + 15s 心跳事件流
│   └── app_state.py          依赖注入容器（bus / 任务强引用 / 占位集合）
└── core/
    ├── config.py             ⭐ 配置中心：环境差异化 + 生产强校验
    └── logging.py            structlog 配置
```

### 3.3 数据血缘：三个结构 + 一个状态

```
Plan（工单本）
 ├── steps: [Step, Step, ...]        ← LLM 规划 + validator 校验后定稿
 │     每个 Step 完成后产出 Artifact
 ├── artifacts: {key: Artifact}      ← 全局产物仓库（Agent 间消息总线）
 └── status: PlanStatus              ← 生命周期唯一权威（随 checkpoint）

AgentState（LangGraph 状态）装 Plan / artifacts / current_step / revision_count
事件（PlanEvent）走 SSE 旁路，不进 state
```

**一句话**：Plan 是"控制面"（做什么），artifacts 是"数据面"（已产出什么），state 是"入账总账"，SSE 是"直播弹幕"。

---

## 第 4 章 数据模型四件套（代码逐行）

### 4.1 Step：一张工单

```python
class Step(BaseModel):
    id: str                    # "s1"，SSE 事件 task_id 用它归位
    name: str                  # "解析论文"（人类可读，审批清单渲染用）
    type: str                  # paper_parse / repo_discovery / code_run / report
    agent: str                 # ⭐ 派工钥匙：workers 名册查表路由
    status: StepStatus = StepStatus.pending   # 运行时状态，只由代码填
    required_artifacts: list[str] = []        # 领什么料（必须有 producer）
    output_artifacts: list[str] = []          # 交什么货（跨步骤不能重名）
    inputs: dict[str, object] = {}            # 执行参数（planner 拿 intent 实体确定性填充）
    timeout_seconds: int = 600                # 沙箱 kill 用
    result / error / started_at / finished_at # 运行时痕迹
```

**LLM 边界（"代码约束 LLM"在数据模型层的落地）**：
- LLM 只能填**设计字段**：name / type / agent / artifacts 契约——且必须过 validator 两道闸；
- `status / result / error` 是**运行时状态**，全部由 `execute_step` 节点填——**LLM 全程摸不到**。

状态机（5 态，从原 8 态瘦身）：
```
pending → in_progress → completed（正常）
                ↓
             failed（3 次尝试全败，fail-fast）
pending → canceled（用户取消，剩余步骤由 execute_step 标记）
```
砍掉的三态及理由：`ready`（链式下"轮到它"就是 pending）、`blocked`（连坐机制被 fail-fast 取代）、`skipped`（没有真实跳过语义）。

### 4.2 Plan：工单本 + 仓库

```python
class Plan(BaseModel):
    id: str                        # = checkpoint 的 thread_id（断点续跑寻址钥匙）
    status: PlanStatus = pending   # pending/in_progress/completed/failed/canceled
    steps: list[Step] = []         # ⭐ 有序数组：下标即依赖即执行序
    artifacts: dict[str, Artifact] = {}   # ⭐ 全局产物仓库（消息总线）
    user_intent / intent_type / trace_id  # 溯源与日志关联
```

**为什么是"步骤数组"而不是 DAG**（面试高频，见 9.2）：论文复现的任务依赖恒为"前一步"，业务图 100% 是链式。链是 DAG 的特例——**数组下标同时承载拓扑序、依赖序、执行序（四序合一）**，独立的 `dependencies` / `TaskEdge` / 拓扑排序 / 分层并行全部冗余。

### 4.3 Artifact：交接棒

```python
class Artifact(BaseModel):
    key: str                   # "parsed_paper" / "repo_url" / "run_result" / "final_report"
    type: ArtifactType = text  # json/code/url/text/metrics/report/...
    producer_task_id: str = "" # 谁产的（溯源）
    value: Any = None          # 内容
    metadata: dict = {}        # {"ok": True, "attempts": 1, ...}
```

用法：`LibrarianAgent` 产出 `parsed_paper` → `CoderAgent` 消费它产出 `generated_code` → `DataAgent` 消费所有产物产出 `final_report`。**生产者写、消费者按 `required_artifacts` 取**——解耦且可溯源。

### 4.4 AgentState：全局状态（含 reducer）

```python
class AgentState(TypedDict):
    user_input: str
    session_id: str
    plan_id: str
    intent: IntentContext
    plan: Plan
    artifacts: dict[str, Artifact]
    final_report: str
    error: str | None
    current_step: int                            # ⭐ 游标：执行到第几步
    revision_count: Annotated[int, operator.add] # reducer 累加：审批修订计数
```

**⭐ Reducer（`Annotated` 第二参数）——字段的"入账规则"**：
- 无 reducer 的字段：节点返回值 = **新值**，直接覆盖；
- 有 reducer 的字段：节点返回值 = **增量**，框架执行 `reducer(旧值, 增量)`。比如 `revision_count` 用 `operator.add`——approval 的 revise 分支返回 `1`，框架累加；`/resume` 端点读它做"最多改 3 版"的封顶判定。

**注意：state 里没有 `events` 字段**——事件走 `event_sink` 回调直推 SSE（旁路），不进 state。

---

## 第 5 章 一次请求的完整旅程（八站）

以"帮我复现 Attention Is All You Need 论文"为例，跟着数据走一遍。

### 站 0：POST /runs —— 起跑（API 层）

```python
@router.post("/runs", status_code=202)
async def run_one_shot(body: CreatePlanRequest, state: AppState = Depends(get_state)):
    plan_id = str(uuid.uuid4())
    state.bus.subscribe(plan_id)     # 先订阅 SSE（幂等，队列缓冲）
    state.active_runs.add(plan_id)   # 占位（防 SSE 竞态 404）
    state.spawn(_run_full_flow(state, plan_id, body.user_input, body.session_id))
    return ExecuteAccepted(plan_id=plan_id)
```

**逐行讲**：
- `plan_id` 由 API 层**预生成**而不是等 planner——因为 SSE 队列必须先于图执行订阅（防丢事件），而 plan 要等 `planner_node` 才诞生，所以 ID 反过来传进图里（`state["plan_id"]`，planner 用它覆写 `plan.id`）；
- `bus.subscribe` 先建队列：前端返回 202 后立即连 `/stream`，事件不会丢；
- `active_runs` 占位：`/stream` 的存在性检查在"图还没起跑"的窗口不会误报 404；
- `state.spawn`：**强引用**持有后台任务（asyncio 的裸 `create_task` 只持弱引用，会被 GC 静默回收——这是踩过的坑）；
- 返回 **202 Accepted**：任务已受理，结果走 SSE/GET，不走这个响应。

### 站 1：intent —— 意图识别（三路并行 + 独立降级）

```
用户输入 → rule_router 预分类（关键词命中？直接返回，0 次 LLM）
        → 没命中 → 加载 Redis 记忆 → 三路并行调 LLM
```

```python
async def classify(self, raw_query: str, session_id: str = "") -> IntentContext:
    # 第 1 步：规则快速路径（省时间省 token）
    rule_result = route_by_rule(raw_query)
    if rule_result is not None:
        return IntentContext(raw_intent=raw_query, rewritten_intent=raw_query,
                             intent_type=rule_result, source="rule")

    # 第 2 步：加载 Redis 记忆（多轮上下文；Redis 挂了降级 Noop）
    memory_turns = await self.memory.load_recent_turns(session_id)

    # 第 3 步：三路并行 + ⭐ return_exceptions=True（异常当结果返回，逐路判断）
    classify_r, rewrite_r, extract_r = await asyncio.gather(
        self._classify_only(raw_query, memory_turns),      # 路A：意图类型+实体+置信度
        self._rewrite(raw_query, memory_turns),            # 路B：query 重写为专业表述
        self._extract_paper_fields(raw_query, memory_turns),  # 路C：论文标题/arxiv id
        return_exceptions=True,
    )

    # 第 4 步：降级处理（三路纪律不同！）
    if isinstance(classify_r, Exception):        # 路A 失败 = 致命
        return IntentContext(raw_intent=raw_query, intent_type=IntentType.unknown,
                             source="llm_failed")
    rewritten = raw_query if isinstance(rewrite_r, Exception) else rewrite_r   # 路B 降级
    entities = dict(classify_r.get("entities", {}))
    if not isinstance(extract_r, Exception):
        entities.update(extract_r)               # 路C 降级为空 dict
    ...
    return IntentContext(raw_intent=raw_query, rewritten_intent=rewritten,
                         intent_type=classify_r["intent_type"], entities=entities, ...)
```

**为什么三路并行**：三次 LLM 调用相互独立，串行要 3 倍延迟。**为什么独立降级**：重写失败不该连累分类、抽取失败不该连累重写——`return_exceptions=True` 让异常作为结果返回，调用方**逐路判断**：

| 路 | 任务 | 失败语义 |
|---|---|---|
| A 分类 | 意图类型 + 实体 + 置信度 | **致命** → `unknown` → intent_node 路由到 END |
| B 重写 | query 重写为专业表述 | 非致命 → 降级用原 query |
| C 抽取 | `paper_title` / `arxiv_id` | 非致命 → 降级空 dict |

**intent 节点的三个出口**（`Command` 导航）：`plan` 已存在 → 直进执行（图级直连入口，测试复用）；意图不合格 → END（补发 `plan_failed` 事件收流）；合格 → planner。

### 站 2：planner —— 规划（LLM 优先 + 两道闸 + 模板兜底）

```python
async def build_plan(llm: LLMClient, intent: IntentContext) -> Plan:
    steps = await _try_llm_plan(llm, intent)      # 先试 LLM
    if steps is None:
        steps = build_steps_from_template(intent) # 任何失败 → 模板兜底
        logger.info("planner_fallback_to_template intent=%s", intent.intent_type)
    return Plan(id=str(uuid.uuid4()), user_intent=intent.rewritten_intent or intent.raw_intent,
                intent_type=intent.intent_type.value, steps=steps)


async def _try_llm_plan(llm, intent) -> list[Step] | None:
    try:
        steps = await agent_planner.build_steps(llm, intent)   # LLM 出结构化蓝图
    except Exception:                                          # LLM 挂了
        return None
    ok, reason = validate_critical_contracts(steps)            # 闸1：高契约派工
    if not ok:
        return None
    ok, reason = validate_steps(steps)                         # 闸2：产物契约闭合
    if not ok:
        return None
    return steps
```

**两道闸到底拦什么**（`validator.py`）：

```python
CRITICAL_CONTRACTS = {"repo_discovery": "research_coding_agent"}

def validate_steps(steps) -> tuple[bool, str]:
    # a) 非空
    # b) 同一 artifact 不能有多个 producer（写冲突检测）
    # c) 每个 required_artifacts 必须有 producer（防"消费不存在的产物"）

def validate_critical_contracts(steps) -> tuple[bool, str]:
    # repo_discovery 必须派给 research_coding_agent——
    # 该步骤涉及外部 URL 选择，LLM 会编造 URL 或选错文件，破坏可重复性
```

**设计哲学**：原五项校验只剩两项——"依赖存在"和"Kahn 检环"已删，因为**步骤数组在结构上不可能引用不存在的依赖、也不可能成环**。这类错误由数据结构本身杜绝，比运行时校验更可靠。

### 站 3：approval —— 人工审批（interrupt 挂起）

整个节点体只有一件事：

```python
async def approval_node(state: AgentState) -> Command:
    decision = interrupt({           # ⭐ 挂起整张图，等 resume
        "plan": state["plan"].model_dump(mode="json"),
        "revision_count": state.get("revision_count") or 0,
    })
    action = decision.get("action", "") if isinstance(decision, dict) else ""

    if action == "approve":
        return Command(goto="execute_step")
    if action == "revise":
        feedback = decision.get("feedback", "")
        intent = state["intent"]
        if feedback:
            intent.raw_intent = f"{intent.raw_intent}\n[用户修改意见] {feedback}"
        return Command(goto="planner", update={
            "user_input": feedback, "intent": intent, "revision_count": 1,
        })
    plan = state["plan"]                 # abandon（含未知动作兜底）
    plan.status = PlanStatus.canceled
    return Command(goto="report", update={"plan": plan})
```

**三个关键设计**（面试常问）：

1. **节点体只有 `interrupt()` 一件事**：resume 会**从挂起节点第一行完整重跑**——interrupt 之前不能有任何副作用（不调 LLM、不发事件、不改 state），否则恢复时全部重复执行。`plan_awaiting_approval` 事件因此放在 API 层补发（见下）。
2. **interrupt payload 随 checkpoint 持久化**：审批请求（plan 全量 + 已修订次数）重启不丢，`GET /plans/{id}` 随时查回。这是"状态管理只剩一套账本"的根源。
3. **revise 的 feedback 并入 `intent.raw_intent`**：planner 的 prompt 是完整 intent dump，只写 user_input 重规划根本看不到修改意见——追加到原话后面（原话留史、实体保留），新计划才能吸收反馈。**修订上限（3 次）拦在 API 层**（调用侧守门），图内 `revision_count` 只如实记账——职责分工。

**审批事件谁来发**（API 层，唯一出口，绝不重复）：

```python
async def _emit_awaiting_if_suspended(state, plan_id):
    snapshot = await _get_snapshot(state, plan_id)
    if not _is_awaiting(snapshot):   # 判定：挂起节点名出现在 snapshot.next 里
        return
    await state.bus.publish(plan_id, PlanEvent(type=plan_awaiting_approval, data={...}))
```

### 站 4：execute_step —— 执行循环（自环 + 游标）

```python
async def execute_step_node(state: AgentState) -> Command:
    plan = state["plan"]
    idx = state.get("current_step") or 0

    if plan.status == PlanStatus.pending:
        plan.status = PlanStatus.in_progress          # 首次进入执行循环

    if plan.id in canceled:                           # 协作式取消
        _mark_remaining_canceled(plan, idx)
        return Command(goto="report", update={"plan": plan})

    if idx >= len(plan.steps):                        # 全部跑完
        return Command(goto="report", update={"plan": plan})

    step = plan.steps[idx]
    ok = await _run_step_with_retry(plan, step)       # 3 次原地重试
    if not ok:
        return Command(goto="report", update={"plan": plan})   # fail-fast

    return Command(goto="execute_step", update={      # ⭐ 自环：游标 +1
        "plan": plan, "artifacts": plan.artifacts, "current_step": idx + 1,
    })
```

**四条铁律**：

1. **游标唯一推进点在成功 return 里**——`current_step+1` 与步骤完成是**同一瞬间**落 checkpoint，游标不可能停在"做了一半"的位置。**断点续跑粒度 = 步**：崩溃恢复后已完成步骤不重跑。
2. **artifacts 必须随 update 同步**——state 字段是 checkpoint 入账的唯一通道，`plan.artifacts` 的原地累积不会自动反映到 `state["artifacts"]`（这是踩过的坑：report 读 `state["artifacts"]` 拿到空 dict）。
3. **单步 3 次重试 + fail-fast**：防偶发网络/超时抖动；3 次全败说明是必死任务（repo 404 / 论文太冷门），继续重试只会烧钱。链式下下游全依赖本步，跑下去也必然失败——直接收尾。
4. **未注册 Agent 是确定性错误**（重试无意义，1 次都不多跑）——`workers.get(step.agent)` 查不到直接终局失败。

**重试函数的骨架**：

```python
async def _run_step_with_retry(plan: Plan, step: Step) -> bool:
    agent = workers.get(step.agent)
    if agent is None:                                   # 确定性错误：不重试
        step.status = StepStatus.failed
        step.error = f"未注册的 Agent: {step.agent}"
        return False

    step.status = StepStatus.in_progress
    step.started_at = datetime.now(UTC)
    await _emit(plan.id, PlanEventType.task_started, step)

    for attempt in range(1, MAX_STEP_ATTEMPTS + 1):
        try:
            produced = await agent.execute(step, plan.artifacts)   # ⭐ worker 统一接口
        except Exception as exc:                        # Agent 边界统一兜底
            step.error = str(exc)[:500]
            if attempt < MAX_STEP_ATTEMPTS:
                await _emit(plan.id, PlanEventType.task_retrying, step, {...})
            continue
        await _finalize_success(plan, step, produced)   # 产物入账 + 事件
        return True

    step.status = StepStatus.failed                     # 3 次全败：fail-fast
    return False
```

**协作式取消**："不硬杀正在跑的活"——`canceled` 是一个集合，`/cancel` 往里塞 plan_id，execute_step **每步开头看一眼**："当前步骤跑到哪算哪，剩下的标记取消"。已花的算力不浪费。

### 站 5：worker 内部 —— 4 个异构 Agent 怎么干活

`execute_step` 按 `step.agent` 查表路由到 4 个 worker（统一接口 `execute(step, artifacts) → dict[Artifact]`）：

| Agent | 能力构成 | 处理步骤 | LLM 用法 |
|---|---|---|---|
| `LibrarianAgent` | 纯 LLM | `paper_parse` | 生成论文解析 |
| `CoderAgent` | LLM + 沙箱 | `code_generate / code_run / framework_compare` | 生成代码，执行在沙箱 |
| `ResearchCodingAgent` | **确定性工具链 + Harness** | `repo_discovery / code_run` | 只在 Harness 修复阶段用 |
| `DataAgent` | 纯 LLM | `report` | 汇总 artifacts 出报告 |

以 `ResearchCodingAgent` 为例看"确定性工具链"：

```
repo_discovery：GitHub 搜索（search_repos → pick_best_repo，按 star/活跃度过滤）
code_run：
  1. clone 仓库（三策略降级：shallow → full → codeload tar.gz）
  2. 沙箱 create_for_install（bridge 联网）→ 装依赖 → switch_to_run_phase（断网重建）
  3. exec_in 跑入口文件
  4. 跑挂 → Harness.run_with_healing（第 6.3 节详解）
```

### 站 6：report —— 收尾

```python
def _decide_terminal_status(plan: Plan) -> PlanStatus:
    """终态判定。优先级：取消 > 全部成功 > 失败。"""
    if plan.id in canceled or plan.status == PlanStatus.canceled:
        return PlanStatus.canceled
    if all(s.status == StepStatus.completed for s in plan.steps):
        return PlanStatus.completed
    return PlanStatus.failed
```

- 终态事件三选一（`plan_completed / plan_canceled / plan_failed`）——都进 SSE 的终态集合，前端收到即收流；
- `final_report` 缺失（report 步骤失败/被跳过）时，用 `_build_fallback_report` **现场拼降级摘要**（步骤状态清单 + 产物截断展示）——"永远给得出结果"。

### 站 7：回程 —— resume / cancel（409 的三种语义）

**resume 端点**：

```python
@router.post("/plans/{plan_id}/resume", status_code=202)
async def resume_plan(plan_id, body: ResumeRequest, state=Depends(get_state)):
    snapshot = await _get_snapshot(state, plan_id)
    if _plan_from_snapshot(snapshot) is None:
        raise HTTPException(404, "plan 不存在")
    if not _is_awaiting(snapshot):
        raise HTTPException(409, "计划不在等待审批状态")
    if plan_id in state.resuming_runs:
        raise HTTPException(409, "已有 resume 在处理（双击守卫）")
    _validate_revision(snapshot, body)   # revise 前置：feedback 非空 + 未超 3 次
    state.spawn(_resume_run(state, plan_id, {"action": body.action, "feedback": body.feedback}))
```

**`_resume_run` 里唤醒图**：`await state.graph.ainvoke(Command(resume=decision), config=_thread_config(plan_id))`——`interrupt()` 原地返回 decision，approval 节点继续往下走。

**cancel 端点按图所处阶段分流**：
- **审批挂起中** → 转发 `abandon`（resume 唤醒 → report 终态 canceled）；
- **执行中** → 软取消（标记集合，execute_step 下一轮生效）；
- **已终态** → 409（没有可取消的东西）。

**并发控制的两个占位集合**：`active_runs`（初始 run）与 `resuming_runs`（resume 操作）**分开**——图挂起后初始任务还在收尾（发事件/清标记），此时 resume 完全合法，不能被误伤。

### 站 8：SSE —— 全程直播（含心跳）

```python
async def event_stream(bus: EventBus, plan_id: str, timeout_sec: int = 2400):
    queue = bus.subscribe(plan_id)
    last_event_at = asyncio.get_running_loop().time()
    while True:
        try:
            event = await asyncio.wait_for(queue.get(), timeout=HEARTBEAT_SEC)  # 15s
        except TimeoutError:
            if loop.time() - last_event_at >= timeout_sec:
                break                    # 真超时（执行侧崩了）：断流，交给 GET 兜底
            yield ": keepalive\n\n"      # ⭐ 心跳：审批等待无界，"没事件"是常态
            continue
        last_event_at = loop.time()
        yield sse_format(event)
        if event.type in TERMINAL_EVENTS:  # plan_completed/failed/canceled
            break
```

**推拉分工**：SSE 是"尽力推"（没订阅者时事件直接丢，不阻塞执行主流程）；`GET /plans/{id}` 是"永远可查"（checkpoint 权威快照）——两通道互补，前端断了也能补救。

---

## 第 6 章 三大亮点深挖

### 6.1 亮点一：代码约束 LLM（LLM 触点清单）

这是项目的第一设计原则。执行层的每个决策点，问一句"这是 LLM 说了算还是代码说了算"：

| 决策 | 谁说了算 | 怎么约束 |
|---|---|---|
| 出什么步骤 | LLM 提议 → **代码裁决** | validator 两道闸（高契约派工 + 产物契约闭合），不过则模板兜底 |
| 先跑哪步 | **代码** | 数组下标（数学性质，不是 LLM"觉得"） |
| 派给谁 | LLM 填字段 → **代码查表** | workers 名册路由；高契约步骤必须归确定性 Agent |
| 要不要重试 | **代码** | `MAX_STEP_ATTEMPTS = 3` 计数器 |
| 代码挂了怎么修 | LLM 提议 → **代码验收** | Harness 5 道闸（见 6.3） |
| 跑没跑成 | **代码** | exit_code / 仓库指纹 / 指标重算 |
| 计划要不要执行 | **人** | `interrupt()` 挂起，approve / revise / abandon |
| 断点续跑到哪了 | **代码** | `current_step` 游标（随 checkpoint 落盘，LLM 摸不到） |

**LLM 在执行层的触点为零**——它的价值被精确限定在"创造性建议"（怎么规划、怎么修代码、怎么写报告），所有"确定性裁决"都在代码手里。

### 6.2 亮点二：沙箱纵深防御（四层隔离 + 两阶段网络）

**先破一个常见误区**：LLM 根本不"进"沙箱。LLM 是外面的一个 HTTP 接口，它只做一件事——**生成代码文本（字符串）**；把文本塞进容器执行的是你的 Python 进程（worker 代码），真正执行代码的是 Docker 守护进程。

**四层隔离**（创建容器时一次性给齐）：

| 层 | 手段 | 防什么 |
|---|---|---|
| 文件系统 | 只挂载授权过的 workspace（`authorize_mount_path` 路径逃逸校验） | 读宿主机任意文件 |
| 网络 | 运行阶段 `network_mode="none"` **断网** | 外联服务器/偷传数据 |
| 资源 | `mem_limit` + `cpu_quota` + `pids_limit` | 内存爆炸/CPU 打满/进程炸弹 |
| 权限 | `user=nobody` + `cap_drop=ALL` + `no-new-privileges` | 提权 |

**两阶段网络**（解决"装依赖要网 / 跑代码不许网"的矛盾）：

```
阶段1 create_for_install：bridge 网络容器（联网）
    └─ pip install ...（需要访问 PyPI）     资源放宽到 1g
阶段2 switch_to_run_phase：停删旧容器 → 断网重建
    └─ python run.py                      依赖装在挂载卷里，重建不丢
```

```python
# 一次性执行（最简单形态）
container = await asyncio.to_thread(
    self.client.containers.run, image, ["python", "-c", code],
    detach=True,
    network_mode="none",          # 断网
    mem_limit="512m",
    cpu_quota=100_000,            # 1 核
    labels={SANDBOX_LABEL: "true"},
    **secure_docker_kwargs(),     # user=nobody / cap_drop=ALL / no-new-privileges
)
```

细节：docker-py 是**同步 SDK**，统一用 `asyncio.to_thread` 包住避免阻塞事件循环；容器打 `SANDBOX_LABEL` 标签，启动钩子按它**清理孤儿容器**（崩溃恢复的最后一环）。

### 6.3 亮点三：Harness 自愈（5 道闸，项目灵魂）

**场景**：复现仓库的代码跑挂了（ImportError、路径错、版本不兼容）。传统做法要么放弃要么人肉修。Harness 让 LLM 修，但**每次修复都要过 5 道闸**——这也是 Harness 区别于"普通代码执行器"的核心。

主循环骨架（真实代码节选）：

```python
async def run_with_healing(self, workspace, sandbox_id, entrypoint, max_attempts=3):
    original_fingerprint = repo_fingerprint(workspace)     # 全仓文件哈希快照
    code = (Path(workspace) / entrypoint).read_text()      # 入口文件 = LLM 要修的对象

    for attempt in range(1, max_attempts + 1):
        # 闸2a：LLM 说修了但代码没变（SHA256 对比）→ 跳过执行，别白烧
        if attempt > 1 and code_sha256(code) == attempts[-1].code_hash:
            attempts.append(HarnessAttempt(attempt=attempt, exit_code=-1,
                                           error="LLM 修复后代码未变化，跳过执行"))
            continue

        # 闸1：补丁静态校验（防越权：不许 os.system / 写可疑路径……）
        patch_ok, patch_reason = validate_patch(code)
        if not patch_ok:
            code = await self._repair_code(code, patch_reason, attempts)
            continue

        backup = self._backup_files(workspace, entrypoint)  # 备份 + 写入 + 执行
        entry_path.write_text(code, encoding="utf-8")
        result = await self.sandbox.exec_in(sandbox_id, ["python", entrypoint])  # ⭐ 复用容器

        if result.exit_code == 0:
            # 闸3：执行期间偷改文件？（全仓指纹对比，白名单=入口文件本身）
            after_fp = repo_fingerprint(workspace)
            unauthorized = [p for p in detect_unauthorized_changes(original_fingerprint, after_fp)
                            if p != entrypoint]
            if unauthorized:
                code = await self._repair_code(code, f"执行期间偷改文件：{unauthorized}", attempts)
                continue

            # 闸4：指标重算防伪（metrics.json vs 从 predictions.jsonl 重算）
            if metrics_path.exists() and preds_path.exists():
                recomputed = recompute_metrics(str(preds_path))
                metrics_ok, metrics_reason = verify_metrics(reported, recomputed)
                if not metrics_ok:
                    code = await self._repair_code(code, metrics_reason, attempts)
                    continue

            self._restore_files(workspace, backup)          # 还原入口文件
            return HarnessReport(status="passed", attempts=attempts, final_result=result.stdout)

        self._restore_files(workspace, backup)
        # 闸5：熔断（连续同类失败 → 止损，别死循环烧 token）
        should_break, break_reason = self.breaker.should_break(f"python {entrypoint}", result.stderr)
        if should_break:
            return HarnessReport(status="failed", attempts=attempts, reason=break_reason)

        code = await self._repair_code(code, result.stderr or result.stdout or "", attempts)
```

五道闸各自防什么：

| 闸 | 机制 | 防的坏行为 |
|---|---|---|
| 1 | `patch_policy` 静态校验 | LLM 写 `os.system("rm -rf /")` 之类的越权补丁 |
| 2 | SHA256 前后对比（2a 假修复 + 2b 指纹） | ①假装修了白烧一轮；②执行期间偷改源码（如改掉评估逻辑） |
| 3 | 仓库指纹（全文件 hash，入口文件白名单） | 执行期越权写入 |
| 4 | **指标重算**（`metrics_recompute.py` 纯 Python 实现 accuracy/macro_f1/mse/mae） | 跑通了但指标造假（硬编码 `accuracy=0.99`） |
| 5 | 熔断器（window=2 连续同因失败） | 死循环修复同一类错误烧 token |

**负反馈注入**（机制5）：修复 prompt 里带上历史失败记录（哪次、什么错、exit_code）——"LLM 不会在同一个坑里摔三次"。

**关键设计点**：
- **复用持久化沙箱**（`sandbox_id` + `exec_in`），不起新容器——前面 clone + 装依赖的成果不能丢；
- **入口文件加白名单**：Harness 修复的就是入口文件，指纹对比时把它排除，否则"自己修自己报"；
- **备份/还原**：每轮执行前备份、失败后还原，保证下轮从干净状态开始。

---

## 第 7 章 测试体系（106 条，全离线）

### 7.1 分层分布

| 文件 | 数量 | 测什么 |
|---|---|---|
| `test_api.py` | 19 | HTTP 层：审批三动作、双击守卫、修订上限、取消三分支、SSE |
| `test_harness.py` | 20 | 自愈引擎：五道闸各自触发场景 |
| `test_workers.py` | 19 | 四个 Agent 的分发逻辑 + 产物契约 |
| `test_service_graph.py` | 11 | 图编排：自环推进、重试、fail-fast、取消、interrupt 挂起/恢复 |
| `test_planner.py` | 10 | 两道闸 + LLM 优先/兜底 |
| `test_sandbox.py` | 10 | 沙箱生命周期 |
| `test_tools.py` | 8 | GitHub 搜索 / 依赖安装 |
| `test_intent_classifier.py` | 5 | 三路并行 + 降级 |
| `test_models.py` | 2 | 模型默认值 + 序列化往返 |
| `test_e2e.py` | 2 | **全链路**：真 workers + 假外部边界 |

### 7.2 测试原则："不联网、不花钱、不起真容器"

这是套件能进 CI、能"可回归"的前提。四个替身（`tests/conftest.py`）：

| 替身 | 替换什么 | 怎么工作 |
|---|---|---|
| `FakeLLM` | LLM 客户端 | 预设响应按序弹出；`with_structured_output` 返回预设 schema 实例 |
| `fakeredis` | 真 Redis | 内存版异步客户端，解码行为一致 |
| `StubAgent / FakeSandbox` | worker / Docker | 脚本化返回值；沙箱用本地 subprocess 替身 |
| `monkeypatch` | 网络边界 | 把 GitHub/PyPI 调用改为假数据 |

**e2e 测试的价值**：两条全链路用例把"API → 图 → workers → 报告"整条跑通（只把最外层 LLM/GitHub/clone/Docker 换成替身）——**集成问题在 CI 里暴露，而不是留给用户**。

### 7.3 怎么跑

```bash
uv run pytest                      # 全量（约 30 秒）
uv run pytest tests/test_api.py    # 单文件
uv run pytest -k "approval"        # 按关键字
uv run pytest --cov=scholar_agent  # 带覆盖率
```

---

## 第 8 章 CI 与部署

### 8.1 GitHub Actions（`.github/workflows/ci.yml`）

```
push / PR → checkout → 装 uv（含 Python 3.12）→ uv sync --extra dev
         → ruff check .（lint）→ uv run pytest（106 条）
```

- **全离线**：不配任何密钥就能跑红/跑绿——因为外部边界全部被替身替换；
- **uv.lock 锁依赖树**：本地和 CI 得到同一份解析结果；
- 无 CI 时"106 条全绿"只是本机事实，有了 CI 才是**别人能验证的事实**。

### 8.2 Docker 部署

```bash
docker compose up          # app + redis 两个服务
```

- `Dockerfile` + `docker-compose.yml`（app: FastAPI；redis: 7）；
- 挂 `docker.sock`：app 里的沙箱需要调 Docker daemon 起容器；
- **配置分层**（`core/config.py`）：
  - `.env` 存"值"（密钥/端口），`config.py` 定义"结构"（字段/类型/默认值/校验）；
  - 环境枚举 `development / test / production`（`APP_ENV=dev/prod` 都能识别）；
  - **生产强校验**（fail-fast，启动即拦）：

```python
if self.ENVIRONMENT == Environment.PRODUCTION:
    assert self.DEBUG is False, "生产环境 DEBUG 必须为 False"
    assert self.CORS_ORIGINS != ["*"], "生产环境 CORS 不允许通配 '*'"
    assert self.LLM_API_KEY, "生产环境必须设置 LLM_API_KEY"
    assert self.GITHUB_TOKEN, "生产环境必须设置 GITHUB_TOKEN"
```

- **惰性连接 + 降级链**（`main.py`）：Docker/Redis 不在时**启动不失败**——Redis 挂了降级 Noop（失去记忆），checkpointer 挂了降级 InMemory（失去跨重启持久化），但 `/runs` 永远可服务。

---

## 第 9 章 面试速答

### 9.1 十个核心问答

**Q1：这项目解决什么问题？**
论文复现自动化：一句话输入 → 意图识别 → 规划 → 人工审批 → 沙箱执行（搜 repo/装依赖/跑代码）→ 挂了 LLM 自动修（5 道闸验收）→ 出报告，全程 SSE 直播。

**Q2：为什么用步骤数组不用 DAG？**
业务图恒为链式（解析→搜 repo→跑代码→报告）。链是 DAG 的特例——数组下标同时承载拓扑序、依赖序、执行序。砍掉通用 DAG 调度器后图节点 7→5，调度代码 300 行→60 行。

**Q3：怎么保证 LLM 不乱来？**
设计原则"模型负责建议，代码负责约束"。LLM 触点被精确限定：规划提议（两道闸校验+模板兜底）、代码修复（5 道闸验收）、内容生成。执行顺序、Agent 路由、重试、成败判定全部确定性代码；计划执行权在人手里（interrupt）。

**Q4：Harness 自愈怎么工作？**
3 轮重试循环，每轮：补丁静态校验（防越权）→ SHA256 对比（防假修复）→ 执行 → 仓库指纹（防偷改文件）→ 指标重算（防造假）→ 熔断检测（防死循环）。修复 prompt 注入历史负反馈，LLM 不会重复踩坑。

**Q5：人工审批怎么实现的？**
LangGraph 原生 `interrupt()`：approval 节点调用后整图挂起，审批 payload 随 checkpoint 持久化（重启不丢）。`/resume` 传 `Command(resume=决定)` 唤醒。节点体只有 `interrupt()` 一件事——resume 从节点第一行重跑，之前不能有副作用。

**Q6：多 agent 体现在哪？**
4 个异构 Agent（纯 LLM / LLM+沙箱 / 确定性工具链+Harness / 纯 LLM）+ LLM 动态编排步骤 + artifacts 全局仓库做消息总线。不是"多个节点"就叫多 agent，是"**异构能力 + 动态编排 + 消息总线**"三件套。

**Q7：断点续跑怎么做到"粒度=步"？**
`execute_step` 每完成一步 return 一次，`plan` + `current_step` 游标随 checkpoint 原子落盘。游标唯一推进点在成功 return 里——崩溃恢复后已完成步骤不重跑。

**Q8：为什么 Redis 挂了系统还能跑？**
三层降级：意图记忆→Noop、checkpointer→InMemory、容器注册表→进程内存。降级哲学："Redis 挂了不是 /runs 不可用，只是失去跨重启持久化。"`main.py` 的 `_build_checkpointer` 就是这个降级链的实现。

**Q9：沙箱怎么防不可信代码？**
四层隔离（只挂授权目录 / 运行期断网 / 内存 CPU 进程限额 / nobody+cap_drop）＋两阶段网络（装依赖联网、执行断网）＋孤儿容器标签清理。

**Q10：测试怎么做到不联网不花钱？**
FakeLLM（预设响应）+ fakeredis + StubAgent/FakeSandbox + monkeypatch——所有外部边界替身化。这是 106 条能进 CI、能反复回归的前提。

### 9.2 常见追问表（面试官会怎么往下挖）

| 追问 | 答题要点 |
|---|---|
| "interrupt 恢复时为什么不能有副作用？" | resume 从挂起节点第一行**重跑**——之前做的事会全部重复（重复调 LLM / 重复发事件）。所以事件补发放 API 层（`_emit_awaiting_if_suspended`，幂等出口）。 |
| "双击 approve 会怎样？" | `resuming_runs` 占位 + 409 双击守卫；还有一层 409 是"不在等待审批状态"（比如终态后重复 approve）。**409 有三种语义**要分清。 |
| "artifacts 为什么不直接在 plan 上改就行？" | checkpoint 只入账 return 值里的字段——`plan.artifacts` 原地累积不随 update 走的话，`state["artifacts"]` 读出来是空的（真实踩坑）。 |
| "怎么防止 LLM 伪造指标？" | `metrics_recompute.py` 从 `predictions.jsonl` 逐样本重算（纯 Python 实现 accuracy/macro_f1/mse/mae），与 LLM 上报不一致即判失败。 |
| "cancel 是硬的还是软的？" | 软的（协作式）：标记集合，execute_step 每步开头检查，当前步骤跑到哪算哪；审批挂起态则转发 abandon。 |
| "checkpointer 用 Redis 有什么坑？" | 需要 `setup()` 建索引（幂等）；`decode_responses` 等序列化参数有自己的要求，所以用 URL 让 saver 自建连接，不复用业务连接。 |
| "为什么图级直连入口可以不传 checkpointer？" | 直连入口传入现成 plan，不经过 approval → 没有 interrupt 挂起点 → 不需要 checkpointer。只有走 `/runs` 全流程才强依赖它。 |

---

## 第 10 章 命令速查 + 源码阅读路线

### 10.1 命令速查

```bash
# 安装（二选一）
uv sync --extra dev            # 与 CI 一致
pip install -e ".[dev]"

# 测试与质量
uv run pytest                  # 106 条全量（约 30 秒）
uv run ruff check .            # lint
uv run mypy scholar_agent      # 类型检查（基线欠账中，暂未进门禁）

# 起服务（需要 .env 配 LLM_API_KEY；Docker daemon 运行中）
uvicorn scholar_agent.main:app --reload

# 体验一次
curl -X POST http://localhost:8000/api/v1/runs \
  -H "Content-Type: application/json" \
  -d '{"user_input": "复现 Attention Is All You Need 论文"}'
# → {"plan_id": "...", "status": "in_progress"}
# 浏览器开 SSE：GET /api/v1/plans/{plan_id}/stream
# 审批：POST /api/v1/plans/{plan_id}/resume -d '{"action": "approve"}'
```

### 10.2 源码阅读路线（建议按此顺序打开真实文件）

| 序 | 文件 | 带着什么问题读 |
|---|---|---|
| 1 | `models/step.py` + `plan.py` + `artifact.py` | 工单长什么样？产物怎么在 Agent 间传递？ |
| 2 | `state.py` | 游标和 reducer 是什么？为什么没有 events 字段？ |
| 3 | `graph.py` | 5 个节点各干什么？Command 导航 vs 静态边的分工？ |
| 4 | `api/routes.py` | 一个请求从进门到挂起经历了什么？409 的三种语义？ |
| 5 | `agent/intent/classifier.py` | 三路并行怎么降级？为什么 classify 是致命的？ |
| 6 | `agent/planner/planner.py` + `validator.py` | 两道闸各拦什么？模板兜底为什么永远可用？ |
| 7 | `agent/sandbox/docker_executor.py` | 四层隔离怎么给参数？两阶段网络怎么切换？ |
| 8 | `agent/harness/runner.py` | 5 道闸在主循环的哪个位置？ |
| 9 | `api/sse.py` + `agent/llm_client.py` | 事件怎么推？LLM 调用怎么封装？ |
| 10 | `tests/` | 挑 `test_service_graph.py` 和 `test_e2e.py` 看测试怎么驱动整张图 |

---

## 附录：三个常见误区（新手最容易想错的）

1. **"LLM 在沙箱里操作文件"** → 错。LLM 只生成代码**文本**；进容器执行的是你的 Python 进程搬运过去的文本。沙箱隔离的是"不可信代码"，不是"LLM"。
2. **"interrupt 恢复 = 从下一行继续"** → 错。resume 从挂起节点**第一行完整重跑**——所以 interrupt 之前不能有任何副作用。
3. **"plan 状态可以存在内存里"** → 错。状态唯一权威是 **checkpoint**（thread_id = plan_id）——`GET` / `resume` / `cancel` 全部从 `aget_state` 读，重启不丢、天然断点续跑。内存里的只有"占位集合"（active_runs / resuming_runs / canceled），它们本就是进程级的临时守卫。

---

*全书完。本文是单文件百科；如需继续深挖，可对照：`README.md`（快速开始）、各代码文件 docstring、`tests/`（测试即最好的用法文档）。*
